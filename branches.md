# Branches

- `hacked` is the main branch where I apply patches from [dwm patches tab](https://dwm.suckless.org/patches/), or edits I write myself.

- `master` is the branch set to track the upstream dwm repository's `master` branch, in order to pull new changes.
